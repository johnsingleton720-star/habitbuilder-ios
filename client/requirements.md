## Packages
date-fns | Date manipulation and formatting
recharts | Visualizing habit streaks and completion data
framer-motion | Smooth animations for habit completion and page transitions
clsx | Utility for conditional class names (often used with tailwind-merge)
tailwind-merge | Utility for merging tailwind classes (standard with shadcn)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
